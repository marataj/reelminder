services:
  reelminderfrontendapp:
    container_name: reelminder_frontend
    build: .
    image: reelminderfrontendapp:v1
    ports: 
      - 80:80
    expose:
      - 80
    networks:
      - internalnet

networks:
  internalnet:
    driver: bridge